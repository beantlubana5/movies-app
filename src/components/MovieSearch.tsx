import './MovieSearch.css'

const MovieSearch = () => {
  return (
    <form className='MovieSearch'>
        <input type="text" />
        <button>Search</button>
    </form>
  )
};

export default MovieSearch;
